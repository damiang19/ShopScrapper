spring:
  jpa:
    hibernate: ddl-auto=create
  datasource:
    driver-class-name=org:
      testcontainers:
        jdbc: ContainerDatabaseDriver
  config:
    activate:
      on-profile: test

jwt.secret : haha